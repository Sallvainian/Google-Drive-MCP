# Node dependencies
node_modules/
dist/

# Credentials / Tokens
credentials.json
token.json

# Log files
*.log

# Environment variables
.env*

# Backup files (use git history instead)
*.bak
src/backup/

# Bundled output files
repomix-output*

# One-off utility scripts
export-docs.mjs

# Local workspace / session files
.mcp.json

# .claude/ is TRACKED (config, hooks, skills, personalities).
# Only the disposable/large caches under it are ignored:
.claude/audio/
.claude/.agentvibes-backup/
.claude/**/.DS_Store
.claude/*.bak-*
# Symlink into the BMAD-Custom repo, where this skill lives and has its own
# history. Tracking it would store an absolute path that only resolves on one
# machine — see .bmad-loop/profiles/grok.toml on why links dangle here.
.claude/skills/loop-monitoring
.agents/
.playwright/
.playwright-cli/
.playwright-mcp/

# AI tool dotfiles
.cursor/
.codex/

# BMad workspace output
_bmad/
# _bmad-output/ is local except specs/: bmad-loop mounts a fresh worktree for a
# story re-drive and the re-driven session reads the COMMITTED spec, so an
# uncommitted correction never reaches it.
_bmad-output/*
!_bmad-output/specs/

# Archived doc scan-report snapshots
docs/.archive/

# macOS
.DS_Store 
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml
_bmad/render/

# Grok CLI (bmad-loop review adapter) — local tooling, not tracked.
# Tested on grok 1.0.30: Grok discovers repo-scoped skills regardless of
# gitignore state, so this tree does not need tracking (README.md:1589
# claims otherwise; the shipped binary does not behave that way).
# Worktree checkouts get the tree via seed_files in
# .bmad-loop/profiles/grok.toml, not via git.
.grok/
